[project]
name = "cheaprecipe"
version = "0.1.0"
description = "Recipes curated from reduced-price grocery items"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "sqlalchemy",
    "pandas",
    "requests",
    "python-dotenv",
    "openai",
    "pydantic-ai-slim[openai]>=2.43,<3",
    "langfuse",
    "rich",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff",
]

[project.scripts]
cheaprecipe = "cheaprecipe.pipeline:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cheaprecipe"]

[tool.pytest.ini_options]
testpaths = ["tests"]
